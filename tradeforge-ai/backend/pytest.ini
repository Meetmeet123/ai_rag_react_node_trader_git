[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    training: marks tests that train or fine-tune ML models
